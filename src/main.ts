// Entry point: reconcile blocking → start decision loop + safety loop + dashboard.
// See spec §2.3. Implemented Day 3.
async function main(): Promise<void> {
  // eslint-disable-next-line no-console
  console.log("flatline — Day 0 scaffold. Not implemented yet.");
}

main().catch((err) => {
  // eslint-disable-next-line no-console
  console.error(err);
  process.exit(1);
});
