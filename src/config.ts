// Constants, env loading, version pinning. Hard rules from spec §1 live here
// as `as const` exports — .env can only set values that aren't safety bounds.
// Implemented incrementally starting Day 1.
export {};
