# Pinned base image SHA, checksummed Sol CLI install at build time.
# See spec §6.2. Filled in Day 7.
FROM node:20-alpine
WORKDIR /app
# Placeholder — real Dockerfile lands at deploy time.
CMD ["node", "--version"]
